# Plan

## Follow-up plan

1. Crop and scale the supplied transparent logo asset within an overflow-safe mark container.
2. Add an intermediate responsive breakpoint for the hero and process layout, preserving readable type and no horizontal clipping.
3. Build a dedicated `/services` route with concise capability lists and CSS workflow visuals for the three real Forge disciplines.
4. Route homepage navigation and service-card links to the detail page.
5. Run build, lint, rendered HTML checks, and a focused diff review.

## QuantumFlux adaptation plan

1. Create a reusable white-glass card system with readable text, focus states, and restrained hover depth.
2. Convert the homepage service grid to a capability deck and the principles list to a bento layout.
3. Replace the Services concentric-map visual with an original connected signal board.
4. Keep the existing sticky process and user-controlled solution carousel as the site’s only scroll/dynamic narratives.
5. Verify responsive route output, reduced-motion rules, linting, and production build.

1. Add contrast-safe text and glass-surface tokens, with explicit dark CTA form styles.
2. Add active-section navigation and active process-step progression using IntersectionObserver.
3. Replace the static capability outcome grid with an original, keyboard-operable solution-story carousel using existing truthful service copy.
4. Apply restrained motion and responsive layout rules; avoid hero tilt or scroll-linked rotation.
5. Validate rendered HTML, linting, and the new interaction markup.

## Rollback

All changes are isolated to the landing-page component and stylesheet and can be reverted with one commit.

## Performance and visual alignment plan

1. Repair Services navigation spacing and responsive collapse behavior in its scoped header rules.
2. Move generated service imagery out of CSS backgrounds; retain Services visuals as code-native gradients and workflow lines.
3. Create 960px WebP assets and place them as lazy decorative homepage imagery only in the About panel and AI workflow carousel slide.
4. Remove scroll-frame CSS variable writes, blur-based section reveals, and redundant nested navigation blur.
5. Apply one reusable cyan-to-blue heading accent with a contrast-safe fallback.
6. Run diff, lint, build, and server-render checks.
