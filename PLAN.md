# Plan

1. Add contrast-safe text and glass-surface tokens, with explicit dark CTA form styles.
2. Add active-section navigation and active process-step progression using IntersectionObserver.
3. Replace the static capability outcome grid with an original, keyboard-operable solution-story carousel using existing truthful service copy.
4. Apply restrained motion and responsive layout rules; avoid hero tilt or scroll-linked rotation.
5. Validate rendered HTML, linting, and the new interaction markup.

## Rollback

All changes are isolated to the landing-page component and stylesheet and can be reverted with one commit.
